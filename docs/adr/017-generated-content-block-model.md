# ADR 017-generated-content-block-model

## Decision
Phase 7 uses structured, cited content blocks; immutable citations; deterministic confidence labels;
a provider-neutral curriculum planner; human-content locks; and rollback by cloning history to a draft.

## Consequences
Generated content remains reviewable and cannot silently publish or rewrite history.
